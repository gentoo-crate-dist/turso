  name: Sync with upstream
  on:
    push:
    schedule:
      - cron: "44 */6 * * *"
    workflow_dispatch:

  jobs:
    sync-mirror:
      runs-on: ubuntu-latest
      permissions:
        contents: write
      steps:
        - name: Update mirror
          uses: projg2/crate-dist-mirror-action@v2
          with:
            ssh-private-key: ${{ secrets.ssh_key }}
            directories: cli bindings/python extensions/completion simulator sdk-kit
            
            
